# Agent guide — `terraform/traefik/`

The canonical guide for this directory is [`AGENTS.md`](./AGENTS.md). Read it first.

All section content lives in `AGENTS.md`; this file is a pointer so `CLAUDE.md` auto-loaders still surface the path.

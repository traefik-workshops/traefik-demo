# Demo Catalog

> Auto-generated by `make catalog-markdown`. Do not edit by hand — run `make catalog-markdown` to refresh.
> Source of truth: [`catalog.json`](./catalog.json) (regenerated by `make catalog`).

## Sections at a glance

| Section | Modules | Use when | What belongs here |
|---|---|---|---|
| `ai` | 12 | AI/LLM workloads | LLM serving, vector DBs, PII detection, AI gateway dependencies |
| `apps` | 7 | Sample workloads | Demo apps (whoami, httpbin) across platforms |
| `compute` | 24 | Clusters & VMs | Managed Kubernetes and IaaS per cloud provider |
| `observability` | 8 | Metrics, logs, traces | Grafana, Prometheus, Loki, Tempo, Langfuse, OpenTelemetry |
| `security` | 4 | Identity providers | Cognito, EntraID, Keycloak, OCI instance principal |
| `tools` | 9 | Cluster add-ons | ArgoCD, cert-manager, PostgreSQL, Redis, k6, MCP inspector |
| `traefik` | 6 | Traefik Hub installs | Traefik Hub across every platform — always included in every PoC |

Total: **70 modules**, **7 Helm charts**

## Module index

### ai

| Path | Required inputs | Keywords |
|---|---|---|
| [`terraform/ai/23ai/k8s`](./terraform/ai/23ai/k8s) | `namespace` | `oracle`, `23ai`, `database`, `vector` |
| [`terraform/ai/LLMs/runpod`](./terraform/ai/LLMs/runpod) | `runpod_api_key`, `hugging_face_api_key` | `ai`, `llm`, `chatbot`, `copilot`, `gpu`, `runpod` |
| [`terraform/ai/NIMs/runpod`](./terraform/ai/NIMs/runpod) | `runpod_api_key`, `ngc_token` | `nim`, `nvidia`, `topic control`, `content safety`, `jailbreak detection`, `safety` |
| [`terraform/ai/ai-gateway-dependencies/k8s`](./terraform/ai/ai-gateway-dependencies/k8s) | `namespace` | `ai gateway`, `mcp`, `model routing`, `dependencies`, `crds` |
| [`terraform/ai/granite-guardian/runpod`](./terraform/ai/granite-guardian/runpod) | `runpod_api_key`, `hugging_face_api_key` | `granite guardian`, `ibm`, `safety`, `content moderation` |
| [`terraform/ai/knative/k8s`](./terraform/ai/knative/k8s) | `namespace` | `knative`, `scale to zero`, `serverless` |
| [`terraform/ai/milvus/k8s`](./terraform/ai/milvus/k8s) | `namespace` | `vector search`, `rag`, `embeddings`, `milvus`, `vector db` |
| [`terraform/ai/ollama/k8s`](./terraform/ai/ollama/k8s) | *(none)* | `ai`, `llm`, `chatbot`, `copilot`, `ollama`, `model` |
| [`terraform/ai/open-webui/k8s`](./terraform/ai/open-webui/k8s) | *(none)* | `open webui`, `chatbot`, `chat ui`, `model frontend` |
| [`terraform/ai/presidio/k8s`](./terraform/ai/presidio/k8s) | *(none)* | `data masking`, `pii`, `presidio`, `anonymization`, `pii detection` |
| [`terraform/ai/sqlcl/k8s`](./terraform/ai/sqlcl/k8s) | `namespace` | `sqlcl`, `oracle`, `sql`, `mcp` |
| [`terraform/ai/weaviate/k8s`](./terraform/ai/weaviate/k8s) | `namespace` | `vector search`, `rag`, `embeddings`, `weaviate`, `vector db` |

- **k8s** (`k8s`): Deploys an Oracle Database 23ai (Free) StatefulSet with a matching Service into a Kubernetes cluster, optionally fronted by a Traefik Ingress.
- **runpod** (`runpod`): Provisions a set of LLM pods (Llama 3.
- **runpod** (`runpod`): Provisions NVIDIA NIM safety microservices (Topic Control, Content Safety, Jailbreak Detection) on RunPod, gated by per-NIM enable flags.
- **k8s** (`k8s`): Installs the in-cluster dependencies required by the AI Gateway demo (Helm-based bundle).
- **runpod** (`runpod`): Provisions an IBM Granite Guardian safety model pod on RunPod.
- **k8s** (`k8s`): Installs Knative Serving on a Kubernetes cluster (Helm + kubectl CRDs) for the AI Gateway demo.
- **k8s** (`k8s`): Deploys the Milvus vector database into a Kubernetes cluster via Helm.
- **k8s** (`k8s`): Deploys Ollama on a Kubernetes cluster via Helm and optionally pre-pulls a selection of models (Qwen, DeepSeek, Llama).
- **k8s** (`k8s`): Deploys Open WebUI on a Kubernetes cluster via Helm, with optional Traefik ingress, OpenAI-compatible backends, and MCP connection configuration.
- **k8s** (`k8s`): Deploys Microsoft Presidio (PII detection / anonymization) into a Kubernetes cluster via Helm.
- **k8s** (`k8s`): Deploys the SQLcl MCP server as a Kubernetes Deployment + Service, optionally fronted by a Traefik Ingress.
- **k8s** (`k8s`): Deploys the Weaviate vector database into a Kubernetes cluster via Helm.

### apps

| Path | Required inputs | Keywords |
|---|---|---|
| [`terraform/apps/httpbin/k8s`](./terraform/apps/httpbin/k8s) | *(none)* | `demo app`, `sample app`, `httpbin`, `http testing` |
| [`terraform/apps/whoami/cloud-init`](./terraform/apps/whoami/cloud-init) | *(none)* |  |
| [`terraform/apps/whoami/ec2`](./terraform/apps/whoami/ec2) | *(none)* | `demo app`, `sample app`, `whoami`, `ec2` |
| [`terraform/apps/whoami/ecs`](./terraform/apps/whoami/ecs) | `name` | `demo app`, `sample app`, `whoami`, `ecs` |
| [`terraform/apps/whoami/k8s`](./terraform/apps/whoami/k8s) | *(none)* | `demo app`, `sample app`, `whoami` |
| [`terraform/apps/whoami/nutanix`](./terraform/apps/whoami/nutanix) | `vm_name`, `cluster_id`, `subnet_uuid`, `image_id` | `demo app`, `sample app`, `whoami`, `nutanix` |
| [`terraform/apps/whoami/nutanix/image_builder`](./terraform/apps/whoami/nutanix/image_builder) | *(none)* |  |

- **k8s** (`k8s`): > **Status:** stub module.
- **cloud-init** (`cloud-init`): > **Status:** stub module.
- **ec2** (`ec2`): Provisions one or more Traefik `whoami` instances on AWS EC2, wrapping `compute/aws/ec2` and the `whoami/cloud-init` template.
- **ecs** (`ecs`): Provisions Traefik `whoami` services across one or more ECS clusters, wrapping `compute/aws/ecs`.
- **k8s** (`k8s`): Deploys one or more Traefik `whoami` instances on Kubernetes as Deployments + Services, with optional Traefik `IngressRoute`, `Middleware` (strip-prefix), and Traefik Hub `Uplink` resources.
- **nutanix** (`nutanix`): Provisions a Traefik `whoami` VM on Nutanix AHV via `compute/nutanix/vm`, with cloud-init and Prism Central category-based service discovery.
- **image_builder** (`nutanix`): Builds a Whoami qcow2 image with Packer (via `local-exec`) and uploads it to Nutanix as a `nutanix_image`.

### compute

| Path | Required inputs | Keywords |
|---|---|---|
| [`terraform/compute/akamai/lke`](./terraform/compute/akamai/lke) | `cluster_name` | `linode`, `lke`, `akamai`, `kubernetes`, `k8s` |
| [`terraform/compute/aws/ec2`](./terraform/compute/aws/ec2) | `apps` | `aws`, `ec2`, `amazon`, `vm` |
| [`terraform/compute/aws/ecs`](./terraform/compute/aws/ecs) | `name`, `clusters` | `aws`, `ecs`, `fargate`, `amazon`, `containers` |
| [`terraform/compute/aws/eks`](./terraform/compute/aws/eks) | `cluster_name` | `aws`, `eks`, `amazon`, `kubernetes`, `k8s` |
| [`terraform/compute/aws/vpc`](./terraform/compute/aws/vpc) | `name` | `aws`, `vpc`, `amazon`, `networking` |
| [`terraform/compute/azure/aks`](./terraform/compute/azure/aks) | `resource_group_name`, `cluster_name` | `azure`, `aks`, `microsoft`, `kubernetes`, `k8s` |
| [`terraform/compute/digitalocean/doks`](./terraform/compute/digitalocean/doks) | `cluster_name` | `digitalocean`, `doks`, `do kubernetes`, `kubernetes`, `k8s` |
| [`terraform/compute/gcp/gke`](./terraform/compute/gcp/gke) | `cluster_name` | `gcp`, `gke`, `google cloud`, `kubernetes`, `k8s`, `google` |
| [`terraform/compute/imported/k8s`](./terraform/compute/imported/k8s) | `kubeconfig` | `bring your own`, `existing cluster`, `imported`, `kubeconfig`, `byoc`, `external cluster` |
| [`terraform/compute/nutanix/categories`](./terraform/compute/nutanix/categories) | `categories` | `nutanix`, `categories`, `tagging` |
| [`terraform/compute/nutanix/fip`](./terraform/compute/nutanix/fip) | `name`, `external_subnet_uuid`, `type` | `nutanix`, `floating ip`, `fip` |
| [`terraform/compute/nutanix/nkp`](./terraform/compute/nutanix/nkp) | `cluster_name`, `nutanix_cluster_id`, `nutanix_prism_element_cluster_name`, `bastion_subnet_uuid`, `external_subnet_uuid`, `cluster_subnets`, `vpc_uuid`, `control_plane_vip`, `lb_ip_range`, `nutanix_username`, `nutanix_password`, `nutanix_endpoint`, `nkp_image_name`, `nkp_image_uuid` | `nutanix`, `nkp`, `on-prem`, `hybrid`, `private cloud` |
| [`terraform/compute/nutanix/nkp/bastion_image`](./terraform/compute/nutanix/nkp/bastion_image) | *(none)* |  |
| [`terraform/compute/nutanix/nkp/kommander`](./terraform/compute/nutanix/nkp/kommander) | *(none)* |  |
| [`terraform/compute/nutanix/nkp/registry`](./terraform/compute/nutanix/nkp/registry) | `cluster_name`, `nutanix_cluster_id`, `subnet_uuid`, `registry_image_uuid` |  |
| [`terraform/compute/nutanix/nkp/registry_image`](./terraform/compute/nutanix/nkp/registry_image) | *(none)* |  |
| [`terraform/compute/nutanix/storage_container`](./terraform/compute/nutanix/storage_container) | `name`, `cluster_ext_id` | `nutanix`, `storage` |
| [`terraform/compute/nutanix/subnet`](./terraform/compute/nutanix/subnet) | `name`, `cluster_id`, `vlan_id`, `subnet_cidr`, `gateway_ip` | `nutanix`, `subnet`, `vlan` |
| [`terraform/compute/nutanix/vm`](./terraform/compute/nutanix/vm) | `name`, `cluster_uuid`, `subnet_uuid`, `image_uuid` | `nutanix`, `vm`, `on-prem`, `private cloud` |
| [`terraform/compute/nutanix/vpc`](./terraform/compute/nutanix/vpc) | `vpc_name`, `external_subnet_uuid` | `nutanix`, `vpc`, `on-prem`, `networking` |
| [`terraform/compute/oracle/oke`](./terraform/compute/oracle/oke) | `cluster_name`, `compartment_id` | `oracle`, `oke`, `oci`, `kubernetes`, `k8s`, `always free` |
| [`terraform/compute/runpod/auth`](./terraform/compute/runpod/auth) | `runpod_api_key`, `ngc_token` | `runpod`, `ngc`, `registry credentials` |
| [`terraform/compute/runpod/pod`](./terraform/compute/runpod/pod) | `runpod_api_key`, `pods` | `runpod`, `gpu`, `pod` |
| [`terraform/compute/suse/k3d`](./terraform/compute/suse/k3d) | `cluster_name` | `on-prem`, `local`, `no cloud`, `k3d`, `kind`, `kubernetes` |

- **lke** (`akamai`): Provisions an Akamai/Linode Kubernetes Engine (LKE) cluster with optional GPU pool, HA control plane, and extra worker pools.
- **ec2** (`aws`): Provisions a fleet of EC2 instances from an `apps` map (each app supports replicas, Docker-based user-data, optional VPC creation).
- **ecs** (`aws`): Provisions one or more ECS clusters and the underlying task definitions/services from a nested `clusters` map.
- **eks** (`aws`): Provisions an EKS cluster (and node groups) via the upstream community EKS module, with demo-friendly defaults.
- **vpc** (`aws`): Provisions an AWS VPC with public and private subnets (via the community `terraform-aws-modules/vpc` module) and a demo security group.
- **aks** (`azure`): Provisions an Azure Kubernetes Service (AKS) cluster with optional GPU node pool and extra worker pools (one per `worker_nodes` entry).
- **doks** (`digitalocean`): Provisions a DigitalOcean Kubernetes (DOKS) cluster with optional autoscaling and extra worker pools.
- **gke** (`gcp`): Provisions a Google Kubernetes Engine (GKE) cluster with optional GPU pool and extra worker pools.
- **k8s** (`imported`): Bring-your-own Kubernetes cluster: adapt an existing cluster's kubeconfig to the same output shape this repo's managed-k8s modules expose (EKS, AKS, DOKS, .
- **categories** (`nutanix`): Creates Nutanix Prism Central categories and their values from a map.
- **fip** (`nutanix`): Allocates a Nutanix Floating IP and associates it with a VM NIC or a VPC private IP.
- **nkp** (`nutanix`): Provisions a Nutanix Kubernetes Platform (NKP) cluster end-to-end: bastion VM, control plane VIP, optional FIP, Kommander, and kubeconfig extraction.
- **bastion_image** (`nutanix`): Extracts the NKP bastion image from the NKP bundle and uploads it to Nutanix as a `nutanix_image`.
- **kommander** (`nutanix`): Provisions a Floating IP for the Kommander/Traefik LoadBalancer service on an NKP cluster and wires the in-cluster `LoadBalancer` Service to it.
- **registry** (`nutanix`): Provisions a self-hosted container registry VM on Nutanix for use as an NKP registry mirror.
- **registry_image** (`nutanix`): Extracts the NKP registry image from the NKP bundle and uploads it to Nutanix as a `nutanix_image`.
- **storage_container** (`nutanix`): Creates a Nutanix storage container with configurable replication factor, compression, and erasure coding.
- **subnet** (`nutanix`): Creates a Nutanix subnet (VLAN-backed) with optional external flag and DNS configuration.
- **vm** (`nutanix`): Provisions a Nutanix VM from a source image, with optional cloud-init user data, static IP, and Prism Central categories.
- **vpc** (`nutanix`): Creates a Nutanix VPC and a map of overlay subnets, with configurable DNS and externally routable prefixes.
- **oke** (`oracle`): Provisions an Oracle Kubernetes Engine (OKE) cluster with optional extra node pools.
- **auth** (`runpod`): Creates a RunPod registry auth (using NGC credentials) via the RunPod GraphQL API.
- **pod** (`runpod`): Creates a set of RunPod pods from a map definition, optionally using a registry auth for private images and forwarding HuggingFace / NGC tokens.
- **k3d** (`suse`): Provisions a local k3d (k3s-in-Docker) cluster using the `SneakyBugs/k3d` provider, with configurable workers, ports, volumes, host aliases, and registry mirroring.

### observability

| Path | Required inputs | Keywords |
|---|---|---|
| [`terraform/observability/grafana-loki/k8s`](./terraform/observability/grafana-loki/k8s) | `namespace` | `loki`, `logs`, `logging` |
| [`terraform/observability/grafana-stack/k8s`](./terraform/observability/grafana-stack/k8s) | `namespace`, `dashboards` | `monitoring`, `observability`, `grafana`, `prometheus`, `dashboards`, `metrics` |
| [`terraform/observability/grafana-tempo/k8s`](./terraform/observability/grafana-tempo/k8s) | `namespace` | `tracing`, `tempo`, `distributed tracing` |
| [`terraform/observability/grafana/k8s`](./terraform/observability/grafana/k8s) | `namespace`, `prometheus`, `tempo`, `loki`, `dashboards` | `grafana`, `dashboards`, `visualization` |
| [`terraform/observability/grafana/k8s/dashboards/aigateway`](./terraform/observability/grafana/k8s/dashboards/aigateway) | `name`, `namespace` |  |
| [`terraform/observability/langfuse/k8s`](./terraform/observability/langfuse/k8s) | *(none)* | `ai observability`, `llm tracing`, `langfuse`, `model tracing` |
| [`terraform/observability/opentelemetry/k8s`](./terraform/observability/opentelemetry/k8s) | *(none)* | `tracing`, `opentelemetry`, `otel`, `telemetry`, `collector` |
| [`terraform/observability/prometheus/k8s`](./terraform/observability/prometheus/k8s) | `namespace` | `prometheus`, `metrics`, `kube-prometheus` |

- **k8s** (`k8s`): Deploys Grafana Loki on Kubernetes via Helm.
- **k8s** (`k8s`): Deploys a full Grafana + Prometheus stack (kube-prometheus-stack) with optional Traefik ingress and demo dashboards.
- **k8s** (`k8s`): Deploys Grafana Tempo on Kubernetes via Helm.
- **k8s** (`k8s`): Deploys Grafana on Kubernetes via Helm, with wired Prometheus / Tempo / Loki datasources, optional Traefik ingress, optional image renderer, and toggleable demo dashboards.
- **aigateway** (`k8s`): > **Status:** dashboard fragment.
- **k8s** (`k8s`): Deploys Langfuse (LLM observability) on Kubernetes via the `langfuse/langfuse-k8s` Helm chart, with headless org/project/admin seeding and an optional Traefik IngressRoute.
- **k8s** (`k8s`): Deploys the OpenTelemetry Collector via Helm, configured with per-backend pipelines (Prometheus, Loki, Tempo, New Relic, Dash0, Honeycomb, LangSmith, Langfuse).
- **k8s** (`k8s`): Deploys Prometheus on Kubernetes via Helm (kube-prometheus-stack), with an optional Traefik scrape job and ingress.

### security

| Path | Required inputs | Keywords |
|---|---|---|
| [`terraform/security/cognito`](./terraform/security/cognito) | *(none)* | `sso`, `oidc`, `cognito`, `aws`, `user pool`, `auth` |
| [`terraform/security/entraid`](./terraform/security/entraid) | *(none)* | `sso`, `oidc`, `azure ad`, `entraid`, `entra`, `microsoft` |
| [`terraform/security/keycloak/k8s`](./terraform/security/keycloak/k8s) | `namespace`, `users` | `sso`, `oidc`, `keycloak`, `in-cluster idp`, `generic`, `auth` |
| [`terraform/security/oci-instance-principal`](./terraform/security/oci-instance-principal) | `compartment_id` | `oci`, `oracle`, `instance principal`, `iam` |

- **cognito** (``): Provisions an AWS Cognito User Pool, a domain, an App Client, and a set of demo users.
- **entraid** (``): Provisions an Azure AD (Entra ID) Application, a client secret, and a set of demo users.
- **k8s** (`k8s`): Deploys Keycloak on Kubernetes, seeds users + groups + claims, mints per-user access tokens, and stores them as Kubernetes Secrets.
- **oci-instance-principal** (``): Creates an OCI dynamic group and policy so instances in a compartment can authenticate as instance principals (no static API keys).

### tools

| Path | Required inputs | Keywords |
|---|---|---|
| [`terraform/tools/argocd/k8s`](./terraform/tools/argocd/k8s) | `namespace`, `admin_password` | `gitops`, `argocd`, `continuous deployment`, `cd` |
| [`terraform/tools/cert-manager/k8s`](./terraform/tools/cert-manager/k8s) | `namespace` | `tls`, `certificates`, `cert-manager`, `lets-encrypt`, `acme` |
| [`terraform/tools/cloudflare`](./terraform/tools/cloudflare) | `zone_id`, `domain` | `dns`, `cloudflare`, `dns record` |
| [`terraform/tools/k6-operator/k8s`](./terraform/tools/k6-operator/k8s) | `namespace` | `load test`, `k6`, `performance test`, `stress test`, `grafana` |
| [`terraform/tools/k6-operator/k8s/loadgen/aigateway`](./terraform/tools/k6-operator/k8s/loadgen/aigateway) | `apis`, `users`, `keycloak_url`, `keycloak_client_id`, `keycloak_client_secret` |  |
| [`terraform/tools/mcp-inspector/k8s`](./terraform/tools/mcp-inspector/k8s) | `namespace` | `mcp`, `mcp inspector`, `debugger` |
| [`terraform/tools/nginx/k8s`](./terraform/tools/nginx/k8s) | `namespace` | `nginx`, `ingress controller` |
| [`terraform/tools/postgresql/k8s`](./terraform/tools/postgresql/k8s) | `namespace` | `database`, `postgresql`, `postgres`, `db`, `sql` |
| [`terraform/tools/redis/k8s`](./terraform/tools/redis/k8s) | `namespace` | `cache`, `redis`, `kv`, `key-value` |

- **k8s** (`k8s`): Deploys ArgoCD on Kubernetes via Helm with an explicit admin password and optional Traefik ingress.
- **k8s** (`k8s`): Deploys cert-manager on Kubernetes via Helm.
- **cloudflare** (``): Creates a single Cloudflare DNS record (A or CNAME) with optional proxying.
- **k8s** (`k8s`): Deploys the Grafana k6 Operator on Kubernetes via Helm.
- **aigateway** (`k8s`): Renders k6 `TestRun` manifests for the AI-Gateway load test (per-user JWT, multi-turn conversations across one or more model APIs).
- **k8s** (`k8s`): Deploys the MCP Inspector UI on Kubernetes as a Deployment + Service, optionally fronted by a Traefik Ingress.
- **k8s** (`k8s`): Deploys NGINX on Kubernetes via Helm.
- **k8s** (`k8s`): Deploys PostgreSQL on Kubernetes via Helm with a configurable password and database name.
- **k8s** (`k8s`): Deploys Redis on Kubernetes via Helm with a configurable password and replica count.

### traefik

| Path | Required inputs | Keywords |
|---|---|---|
| [`terraform/traefik/cloud-init`](./terraform/traefik/cloud-init) | `traefik_hub_version` | `traefik`, `cloud-init`, `vm` |
| [`terraform/traefik/ec2`](./terraform/traefik/ec2) | *(none)* | `traefik`, `hub`, `ec2`, `aws` |
| [`terraform/traefik/ecs`](./terraform/traefik/ecs) | *(none)* | `traefik`, `hub`, `ecs`, `fargate`, `aws` |
| [`terraform/traefik/k8s`](./terraform/traefik/k8s) | `namespace` | `traefik`, `hub`, `kubernetes`, `api gateway`, `ai gateway`, `mcp gateway` |
| [`terraform/traefik/nutanix`](./terraform/traefik/nutanix) | `vm_name`, `cluster_id`, `subnet_uuid`, `image_id` | `traefik`, `hub`, `nutanix`, `on-prem` |
| [`terraform/traefik/shared`](./terraform/traefik/shared) | *(none)* | `traefik`, `hub` |

- **cloud-init** (``): Renders a cloud-init template that installs and starts Traefik Hub on a VM, with optional Keepalived VRRP, OTLP export, performance tuning, and DNS Traefiker registration.
- **ec2** (``): Provisions one or more Traefik Hub instances on AWS EC2, wiring in `traefik/shared` (config extraction) and `traefik/cloud-init` (boot script).
- **ecs** (``): Provisions Traefik Hub on AWS ECS, wiring in `traefik/shared` (config extraction) and `compute/aws/ecs` (service deployment).
- **k8s** (``): Deploys Traefik Hub on Kubernetes via Helm, with the full feature-flag matrix (API Gateway, AI Gateway, MCP Gateway, API Management, Knative provider, etc.
- **nutanix** (``): Provisions a Traefik Hub VM on Nutanix AHV via `compute/nutanix/vm`, wiring in `traefik/shared` (config) and `traefik/cloud-init` (boot script).
- **shared** (``): Helm-template-extraction module that renders the upstream Traefik Helm chart and exposes the CLI args, env vars, ports, image refs, and static config as outputs.

## Helm charts

| Chart | Required values | Dependencies | Keywords |
|---|---|---|---|
| [`helm/ai-gateway`](./helm/ai-gateway) | *(none)* | `presidio`, `weaviate`, `embeddings` | `traefik`, `ai-gateway`, `llm`, `demo`, `traefik ai gateway`, `ai middleware` |
| [`helm/airlines`](./helm/airlines) | *(none)* | `keycloak`, `hoppscotch`, `ai-gateway` | `traefik`, `airlines`, `demo`, `umbrella`, `airlines demo`, `full demo` |
| [`helm/dns-traefiker`](./helm/dns-traefiker) | *(none)* | *(none)* | `traefik`, `dns`, `cloudflare`, `automation`, `wildcard dns for demos`, `*.demo domain` |
| [`helm/embeddings`](./helm/embeddings) | *(none)* | *(none)* | `traefik`, `ai`, `embeddings`, `infinity`, `embedding server`, `rag embeddings` |
| [`helm/hoppscotch`](./helm/hoppscotch) | *(none)* | *(none)* | `traefik`, `api`, `testing`, `demo`, `api testing ui`, `hoppscotch` |
| [`helm/keycloak`](./helm/keycloak) | *(none)* | *(none)* | `traefik`, `keycloak`, `oidc`, `auth`, `demo`, `self-hosted keycloak` |
| [`helm/presidio`](./helm/presidio) | *(none)* | *(none)* | `traefik`, `ai`, `pii`, `presidio`, `demo`, `pii detection backend` |

> **Umbrella charts**: `helm/airlines` and `helm/ai-gateway` bundle their subcharts.
> If you install an umbrella, do NOT also install its subcharts standalone.
> Gate sub-features via `enabled: true|false` in `values.yaml`.

## Common PoC stacks

Starting points for common prospect patterns.
Always add `terraform/traefik/shared` — it is required for every PoC.

### AWS — API management + SSO + observability

Full API portal with Cognito auth and Grafana metrics/logs.

**Modules:**
- `terraform/compute/aws/eks`
- `terraform/traefik/shared`
- `terraform/traefik/k8s`
- `terraform/security/cognito`
- `terraform/observability/grafana-stack/k8s`

**Charts:**
- `helm/airlines`

### Azure — AI chatbot + PII masking (HIPAA)

CPU LLM with PII scrubbing before model, EntraID OIDC, LLM tracing.

**Modules:**
- `terraform/compute/azure/aks`
- `terraform/traefik/shared`
- `terraform/traefik/k8s`
- `terraform/security/entraid`
- `terraform/ai/ollama/k8s`
- `terraform/ai/presidio/k8s`
- `terraform/ai/open-webui/k8s`
- `terraform/observability/langfuse/k8s`

### GCP — MCP gateway for internal AI agents

MCP gateway with per-agent authz, Keycloak IdP, MCP inspector for debugging.

**Modules:**
- `terraform/compute/gcp/gke`
- `terraform/traefik/shared`
- `terraform/traefik/k8s`
- `terraform/security/keycloak/k8s`
- `terraform/ai/ai-gateway-dependencies/k8s`
- `terraform/tools/mcp-inspector/k8s`
- `terraform/apps/httpbin/k8s`
- `terraform/apps/whoami/k8s`

### Local (k3d) — quick dev demo

No cloud account needed. Fastest path to a running demo.

**Modules:**
- `terraform/compute/suse/k3d`
- `terraform/traefik/shared`
- `terraform/traefik/k8s`

**Charts:**
- `helm/airlines`

---

## Adding to the catalog

New modules and charts appear automatically after `make catalog` is run.
Update [`signals.yaml`](./signals.yaml) to add extra synonym keywords for new entries.
CI fails if `catalog.json` drifts from the tree — run `make catalog` before committing.


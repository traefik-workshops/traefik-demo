terraform {
  required_version = ">= 1.3"

  required_providers {
    random = {
      source  = "hashicorp/random"
      version = "~> 3.6"
    }
    # TODO(new-module): add the cloud-specific IdP provider (aws / azuread / oci).
  }
}

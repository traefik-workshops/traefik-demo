terraform {
  required_version = ">= 1.3"

  required_providers {
    # TODO(new-module): add the cloud provider this cluster runs on.
    # Match the version constraint used in sibling compute/<cloud>/* modules.
  }
}

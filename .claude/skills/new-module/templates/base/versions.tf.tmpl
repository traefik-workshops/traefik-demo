terraform {
  required_version = ">= 1.3"

  # TODO(new-module): declare required_providers for every provider this module
  # actually uses. Leave empty only if the module truly has no provider deps.
  required_providers {}
}

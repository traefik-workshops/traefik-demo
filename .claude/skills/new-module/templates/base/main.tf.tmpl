# {{NAME}} — {{PURPOSE}}
#
# This module currently has no resources. If that stops being true,
# move toward the closest sibling module's pattern.

# TODO(new-module): add resources, or document why this module is template/data-only in README.md.

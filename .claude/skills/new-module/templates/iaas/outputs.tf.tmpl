output "id" {
  description = "Cloud-specific identifier for the {{NAME}} resource."
  # TODO(new-module): wire to resource.
  value = ""
}

# TODO(new-module): add public_ip / endpoint / etc. as the module exposes them.

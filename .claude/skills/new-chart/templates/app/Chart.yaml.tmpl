apiVersion: v2
name: {{NAME}}
description: {{PURPOSE}}
type: application

# Chart version is owned by the release flow — do not edit by hand.
# See /CLAUDE.md "Releases" section.
version: {{REPO_VERSION}}

# App version tracks the upstream app and moves independently of the chart version.
appVersion: "{{APP_VERSION}}"

kubeVersion: ">= 1.27.0-0"

maintainers:
  - name: Traefik SA team
    email: sa@traefik.io

# TODO(new-chart): fill in the upstream source URL(s) for {{NAME}}.
sources:
  - https://github.com/traefik-workshops/terraform-demo-modules

home: https://github.com/traefik-workshops/terraform-demo-modules/tree/main/helm/{{NAME}}

keywords:
  - traefik
  - demo

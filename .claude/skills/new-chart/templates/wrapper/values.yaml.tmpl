# Wrapper chart — values are mostly pass-through to the subchart.
# Override upstream defaults below; the subchart sees them under its own name.

# TODO(new-chart): replace `upstream-chart-name` with the actual subchart name.
upstream-chart-name:
  # All values supported by the upstream chart live here.
